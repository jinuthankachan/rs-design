//! od-prompt — prompt-stack assembly.
//!
//! Ports the assembly logic in upstream `apps/daemon/src/prompts/*` to Rust
//! string composition. The prompt *content/sequencing* is reused data (the moat);
//! only the assembly is code. Coupled to `od-agents` — migrate them together.
//
// TODO(V2 step 6): prompt assembly (ported from apps/daemon/src/prompts/*).
